var Trees := []

for I := 1 .. 5 do
	var T := {}
	for J := 1 .. (I + 3) do
		T['{I}{J}'] := '{I} x {J}'
	end
	Trees:put(T)
	print('Trees[{I}] has size {Trees[I]:size}\n')
end

for T in Trees do
	print('Tree!, #T = {T:size}\n')
	for V, K in T do
		if K = '33' then
			print('\tV = {V}, Z = {Z}, #T = {T:size}\n')
			exit
		end
	end
end
